# PayDance v0.5.12 Settings Breathing Room Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship v0.5.12 as a settings-center polish release that reduces visual crowding, restores mode-specific salary inputs, improves rest-break placement, and centers copyright under the GitHub entry.

**Architecture:** Keep the existing Vue 3/Tauri 2 architecture and the v0.5.11 salary/night-shift logic unchanged. Refine only `SettingsPanel.vue` presentation and source-level tests, then bump release metadata and README notes.

**Tech Stack:** Vue 3, TypeScript, Vitest, Tauri 2, Rust, GitHub Actions.

---

### Task 1: Source Tests For Settings Layout

**Files:**
- Modify: `src/components/SettingsPanel.test.ts`

- [ ] **Step 1: Add failing tests**

Add source tests that require:
- monthly mode gates `monthlySalary` and `workDaysPerMonth`;
- daily mode gates `dailySalary`;
- hourly mode gates `hourlyRate`;
- the old `上下班时间` label is absent;
- `休息扣除` replaces `午休剔除`;
- copyright uses a centered class under the repository card;
- no mini floating mode controls appear in settings.

- [ ] **Step 2: Run red test**

Run: `npm.cmd test -- src/components/SettingsPanel.test.ts`

Expected: FAIL because v0.5.11 shows every salary field and still has the crowded work-time/rest-break layout.

### Task 2: SettingsPanel Refinement

**Files:**
- Modify: `src/components/SettingsPanel.vue`

- [ ] **Step 1: Dynamic salary inputs**

Show only the inputs required by the selected salary mode:
- `monthly`: monthly salary and work days per month;
- `daily`: daily salary;
- `hourly`: hourly rate.

- [ ] **Step 2: Work/rest layout**

Remove the `上下班时间` subheading. Keep start/end fields directly under workday selection. Replace the standalone `午休剔除` row with a lighter `休息扣除` row and only expand start/end rest inputs when enabled.

- [ ] **Step 3: Reduce crowding**

Make the four groups calmer by reducing nested-card feel: lighter group backgrounds, smaller gaps, lower input heights, and fewer internal labels. Keep four top-level groups.

- [ ] **Step 4: About alignment**

Keep the GitHub entry as the repository affordance and center `©️2026 Mr.Baober` underneath it.

- [ ] **Step 5: Run green test**

Run: `npm.cmd test -- src/components/SettingsPanel.test.ts`

Expected: PASS.

### Task 3: Version And README

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json`
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/Cargo.lock`
- Modify: `src-tauri/tauri.conf.json`
- Modify: `src/lib/app-meta.test.ts`
- Modify: `.github/ISSUE_TEMPLATE.md`
- Modify: `.github/ISSUE_TEMPLATE/bug_report.yml`
- Modify: `.github/workflows/release.yml`
- Modify: `README.md`

- [ ] **Step 1: Bump version to 0.5.12**

Update all version sources and metadata tests.

- [ ] **Step 2: Update README release notes**

Add v0.5.12 notes focused on settings center polish and note that v0.5.11 Release was superseded.

- [ ] **Step 3: Verify version consistency**

Run: `npm.cmd run version:check`

Expected: PASS with `Version consistency verified: 0.5.12`.

### Task 4: Verification And Release

**Files:**
- No additional files.

- [ ] **Step 1: Run full verification**

Run:

```powershell
npm.cmd test
npm.cmd run build
npm.cmd run version:check
npm.cmd audit --omit=dev
Push-Location src-tauri
cargo fmt --all -- --check
cargo check
cargo clippy --all-targets -- -D warnings
Pop-Location
git diff --check
```

- [ ] **Step 2: Confirm release state**

Verify `gh release view v0.5.11` fails and `v0.5.12` is the new release after tagging.

- [ ] **Step 3: Commit, push, tag, and release**

Commit to `main`, push, tag `v0.5.12`, push the tag, wait for CI/Release, and verify release assets.

