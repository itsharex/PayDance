{
  "name": "pay-dance",
  "private": true,
  "version": "0.5.7",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "test": "vitest run",
    "preview": "vite preview",
    "release:check": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/ensure-release-unlocked.ps1",
    "prebuild:exe": "npm.cmd run release:check",
    "build:exe": "tauri build --no-bundle",
    "prebuild:installer": "npm.cmd run release:check",
    "build:installer": "tauri build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-opener": "^2.5.4",
    "@tauri-apps/plugin-store": "^2.0.0",
    "@vitejs/plugin-vue": "^5.2.0",
    "lucide-vue-next": "^0.468.0",
    "tailwindcss": "^4.0.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.5",
    "vue-tsc": "^2.1.0"
  }
}
